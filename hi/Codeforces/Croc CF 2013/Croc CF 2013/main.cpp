#include <iostream>
#include <set>
#include <utility>
#include <cmath>
#include <cstdlib>

#define rep(i,n) for (int i = 0; i < n; i++)
#define ll long long
using namespace std;

int main(int argc, const char * argv[]) {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    int n, m;
    cin >> n >> m;
    int a[n];
    int count = 0;
    rep(i, n) cin >> a[i];
    
    cout << count << endl;
    return 0;
}
